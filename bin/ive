#!/usr/bin/env ruby

require "thor"
require "ive"

class IveRunner < Thor
  desc "list", "Show all the possible version bumps."
  def list
    Ive::Base.list
  end

  desc "major", "Perform a major version bump."
  def major
  end

  desc "minor", "Perform a minor version bump."
  def minor
  end

  desc "patch", "Perform a patch version bump."
  def patch
  end

  desc "build", "Perform a build version bump."
  def build
  end
end

IveRunner.start
